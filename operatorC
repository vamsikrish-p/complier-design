#include <stdio.h>

int main() {
    char s[100];
    int i=0;

    printf("Enter expression: ");
    fgets(s,100,stdin);

    while(s[i]){
        if(s[i]=='+'||s[i]=='-'||s[i]=='*'||s[i]=='/')
            printf("Operator: %c\n", s[i]);
        i++;
    }
    return 0;
}
